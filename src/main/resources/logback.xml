<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- Register %colorLine before any PatternLayout compiles its pattern. -->
  <conversionRule conversionWord="colorLine" converterClass="com.openiv.backend.logging.ColorLineConverter"/>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
      <pattern>%colorLine(%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] rid=%X{rid:-none} %logger{36} - %msg%n)</pattern>
    </encoder>
  </appender>

  <!-- AUDIT logger: one JSON object per line. Keep this stream separate so SIEM ingestion
       is simple, and so security events survive application-log routing mistakes. In
       production this should tail to a write-once append-only store. -->
  <appender name="AUDIT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>logs/audit.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>logs/audit-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
      <maxHistory>365</maxHistory>
      <totalSizeCap>50GB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <pattern>%msg%n</pattern>
    </encoder>
  </appender>

  <!-- Async wrapper so AUDIT writes never block the calling (event loop) thread. -->
  <appender name="AUDIT_ASYNC" class="ch.qos.logback.classic.AsyncAppender">
    <queueSize>8192</queueSize>
    <discardingThreshold>0</discardingThreshold>
    <neverBlock>false</neverBlock>
    <includeCallerData>false</includeCallerData>
    <appender-ref ref="AUDIT_FILE"/>
  </appender>

  <logger name="AUDIT" level="INFO" additivity="false">
    <appender-ref ref="AUDIT_ASYNC"/>
  </logger>

  <logger name="io.netty" level="INFO"/>
  <logger name="io.vertx" level="INFO"/>
  <!-- PDFBox logs missing /GSN resources as ERROR — harmless, just noisy -->
  <logger name="org.apache.pdfbox" level="OFF"/>

  <root level="INFO">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>
