/**
 * HTTP API surface. Sub-packages group handlers by feature (e.g. {@code health}) or
 * by version (e.g. {@code v1}). Shared middleware and error translation lives here.
 */
package com.openiv.backend.api;
