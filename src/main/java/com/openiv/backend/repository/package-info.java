/**
 * Persistence layer. Repositories expose async APIs (typically {@code Future<T>}) and
 * hide the underlying datastore (JDBC, Mongo, etc.) from the service layer.
 */
package com.openiv.backend.repository;
