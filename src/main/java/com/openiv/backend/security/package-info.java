/**
 * Transport-layer security controls: headers, CORS, rate limiting, request IDs, method and
 * content-type guards, audit logging, and the auth gate placeholder.
 *
 * <p>Install via {@link com.openiv.backend.api.ApiRouter}; the order matters. See that class
 * for the canonical pipeline.
 */
package com.openiv.backend.security;
